source 'https://rubygems.org'
gem 'rulers', '0.1.0', path: '../rulers' # use the local development version
# execute gem uninstall rulers to uninstall the globally installed version
group :development do
	gem 'rerun' # reload changes to our files
end
